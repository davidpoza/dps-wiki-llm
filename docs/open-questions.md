# Preguntas abiertas

- No determinado a partir del repositorio: politica real de backups de PostgreSQL y vault.
- No determinado a partir del repositorio: entorno de produccion objetivo mas alla de Docker Compose.
- No determinado a partir del repositorio: CI/CD activo.
- No determinado a partir del repositorio: dataset o criterios de evaluacion de calidad LLM/retrieval.
- No determinado a partir del repositorio: presupuesto/coste aceptable por proveedor LLM.
- No determinado a partir del repositorio: politica de retencion de `outputs/**`, snapshots y logs.
- No determinado a partir del repositorio: si `docs/diagrams/workflow.puml` historico debe mantenerse como referencia o eliminarse en una limpieza posterior.
- Pendiente de validacion manual: stack completo `docker compose up --build` en esta maquina.
- Pendiente de validacion manual: `pnpm test` del frontend, porque no se detectaron specs.

